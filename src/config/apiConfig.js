// создаем объект, в котором будет настройка адреса нашего api, куда мы будем делать наши запросы.
const config = {
    url: 'https://aviasales-api.herokuapp.com'
};

// для того, чтобы получить эту конфигурацию потом в других файлах, мы должны сделать export
export default config;